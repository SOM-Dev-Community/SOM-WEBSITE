# Base URL of the LWSOM API (the NestJS app in ../api). Inlined at build time.
NEXT_PUBLIC_API_URL=http://localhost:4000

# Public URL of this site, used for metadata
NEXT_PUBLIC_SITE_URL=http://localhost:3000
